# Application
APP_NAME=Knowledge Chatbot
APP_ENV=development
APP_DEBUG=true
APP_HOST=0.0.0.0
APP_PORT=8000
APP_API_PREFIX=/api/v1
FRONTEND_URL=http://localhost:5173
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Security — NEVER commit real values; use strong random secrets in production
SECRET_KEY=
JWT_SECRET=
INITIAL_ADMIN_EMAIL=admin@example.com
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7
REQUIRE_EMAIL_VERIFICATION=false
EMAIL_VERIFICATION_TOKEN_EXPIRE_MINUTES=1440

# Database
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=chatbot
POSTGRES_PASSWORD=
POSTGRES_DB=chatbot
DATABASE_URL=postgresql+asyncpg://chatbot:chatbot_secret@localhost:5432/chatbot

# Redis / workers
REDIS_URL=redis://localhost:6379/0
ARQ_REDIS_URL=redis://localhost:6379/0

# Vector store
CHROMA_HOST=localhost
CHROMA_PORT=8001
CHROMA_PERSIST_DIR=./storage/chroma

# File storage
STORAGE_PATH=./storage/uploads
TEMP_PATH=./storage/temp
MAX_UPLOAD_SIZE_MB=50
ALLOWED_EXTENSIONS=pdf,docx,txt,csv,md,markdown,ppt,pptx,xls,xlsx,json,html,htm,png,jpg,jpeg,webp,tiff,zip

# LLM — OpenAI
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_DEFAULT_MODEL=gpt-4o-mini
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# LLM — Ollama
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_HOST=http://localhost:11434
OLLAMA_DEFAULT_MODEL=llama3
OLLAMA_EMBEDDING_MODEL=nomic-embed-text

# Default provider selection (openai | ollama)
DEFAULT_LLM_PROVIDER=ollama
DEFAULT_EMBEDDING_PROVIDER=ollama

# RAG defaults
RAG_TOP_K=8
RAG_MMR_LAMBDA=0.5
RAG_SIMILARITY_THRESHOLD=0.35
RAG_CHUNK_SIZE=1000
RAG_CHUNK_OVERLAP=200
RAG_HYBRID_ALPHA=0.7
EMBEDDING_BATCH_SIZE=32
RAG_ALLOW_GENERAL_KNOWLEDGE=false

# Email / SMTP
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASSWORD=
SMTP_USERNAME=
SMTP_FROM=noreply@chatbot.local
SMTP_TLS=false
PASSWORD_RESET_TOKEN_EXPIRE_MINUTES=60

# Rate limiting
RATE_LIMIT_ENABLED=true
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW_SECONDS=60
AUTH_RATE_LIMIT_REQUESTS=20

# API docs & observability
ENABLE_API_DOCS=true
METRICS_REQUIRE_AUTH=false
METRICS_SCRAPE_TOKEN=

# Embedding cache
EMBEDDING_CACHE_ENABLED=true
EMBEDDING_CACHE_TTL_SECONDS=3600

# OCR
TESSERACT_CMD=tesseract

# Docker (optional overrides)
# VITE_API_BASE_URL=http://localhost:8000/api/v1
# OLLAMA_BASE_URL=http://host.docker.internal:11434
# NGINX_PORT=80
